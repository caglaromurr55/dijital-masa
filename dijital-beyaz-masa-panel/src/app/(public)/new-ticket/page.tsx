"use client";

import TicketWizard from "@/components/public/TicketWizard";

export default function NewTicketPage() {
    return (
        <div className="w-full max-w-2xl animate-in fade-in duration-500">
            <TicketWizard />
        </div>
    );
}
